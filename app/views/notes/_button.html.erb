<% id = element.identifier %>
<% unless id.blank? %>
  <span class='note_button' id='note_button_<%= id %>'><%= link_to "#{pluralize(element.notes.count, "note")} +", new_element_note_path(id), :remote => true, :update => id %></span>
<% end %>